<Page x:Class="StudentScheduleClient.AdminPages.AdminAccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentScheduleClient.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminAccountPage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <ListView x:Name="Entities">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="40" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="StudentId" Width="70" DisplayMemberBinding="{Binding StudentId}" />
                    <GridViewColumn Header="Login" Width="100" DisplayMemberBinding="{Binding Login}" />
                    <GridViewColumn Header="Password" Width="200" DisplayMemberBinding="{Binding Password}" />
                    <GridViewColumn Header="">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Edit" Margin="2"
                                            Click="EditButton_Click"
                                            Tag="{Binding}"
                                            />
                                    <Button Content="Delete" Margin="2"
                                            Click="DeleteButton_Click"
                                            Tag="{Binding}"
                                            />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Button
            Grid.Row="2"
            Content="Add" Margin="2"
            Click="AddButton_Click"
            Tag="{Binding}"
           />
    </Grid>
</Page>
